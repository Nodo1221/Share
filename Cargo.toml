[package]
name = "share"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.5", default-features = false, features = ["derive", "std", "help", "usage"] }
pico-args = "0.5.0"
rustls = "0.23.43"
